# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# TypeScript incremental build cache (regenerated by `tsc -b`)
*.tsbuildinfo

# Claude Code — temp files and local (per-developer) settings
tmpclaude-*-cwd
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Android signing secrets — NEVER commit these
*.jks
*.keystore
android/keystore.properties
android/local.properties

devend-upload.keystore
.env.local

# PostHog setup-wizard cache (the usable skill lives in .claude/skills/)
.posthog/
